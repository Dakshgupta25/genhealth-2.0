# Database Configuration
DATABASE_URL=postgresql://postgres:admin123@localhost:5432/genhealth_dev

# Security Settings (MUST be replaced with a strong random key in production)
SECRET_KEY=change_this_to_a_secure_random_secret_in_production
