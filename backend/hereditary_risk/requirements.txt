# Hereditary Risk Engine — Module-specific dependencies
# Install: pip install -r hereditary_risk/requirements.txt

# ML Core
xgboost>=2.0.0
scikit-learn>=1.4.0
shap>=0.44.0

# Data
pandas>=2.0.0
numpy>=1.26.0
ucimlrepo>=0.0.6

# API (FastAPI standalone server — also present in root requirements)
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.0
httpx>=0.27.0

# LLM (Gemini — also present in root requirements)
google-genai>=0.8.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Note: google-genai, fastapi, pydantic, pytest already in root requirements.txt
# This file ensures the module can also be installed standalone.
